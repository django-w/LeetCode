class Solution {
public:
    bool isMatch(string s, string p) {
        for(int i = 0; i < s.length(); i++){
        	
        	if(){

        	}else{
        		return false;
        	}

        }
    }
};